# Grafana Dashboard Provisioning

apiVersion: 1

providers:
  - name: 'ONE-DATA-STUDIO'
    orgId: 1
    folder: 'ONE-DATA-STUDIO'
    folderUid: 'one-data-studio'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
