# Agent API - Production Deployment
# Sprint 1.2: 集成 OpenAI 代理服务
#
# ============================================
# PRODUCTION DEPLOYMENT CONFIGURATION
# ============================================
# This deployment uses pre-built Docker images.
# Application code is built into the image via CI/CD pipeline.
#
# Build the Docker image:
#   docker build -t agent-api:v1.0.0 -f deploy/dockerfiles/agent-api/Dockerfile .
#
# For local development, use docker-compose instead:
#   docker-compose -f deploy/local/docker-compose.yml up agent-api
# ============================================

---
# Agent API Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-api
  namespace: one-data-agent
  labels:
    app: agent-api
    version: v1.1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: agent-api
  template:
    metadata:
      labels:
        app: agent-api
        version: v1.1
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: agent-api
              topologyKey: kubernetes.io/hostname
      securityContext:
        runAsNonRoot: true
        fsGroup: 1000
      containers:
      - name: api
        image: agent-api:v1.0.0  # Use pre-built image instead of python base image with runtime pip install
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        env:
        - name: PYTHONUNBUFFERED
          value: "true"
        - name: DATA_API_URL
          value: "http://data-api.one-data-data.svc.cluster.local:8080"
        - name: MODEL_API_URL
          value: "http://openai-proxy.one-data-model.svc.cluster.local:8000"
        ports:
        - name: http
          containerPort: 8080
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        # Note: Pre-built image contains application code
        # No ConfigMap volume mount needed for production

---
# Agent API Service
apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: one-data-agent
spec:
  selector:
    app: agent-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP

---
# agent Ingress
# SECURITY: TLS is required for production deployments
# Configure cert-manager for automatic certificate management
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent-ingress
  namespace: one-data-agent
  annotations:
    # SECURITY: Force HTTPS redirect
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # Enable cert-manager for automatic TLS certificates
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - agent.example.com
    secretName: agent-tls-secret
  rules:
  - host: agent.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: agent-api
            port:
              number: 8080
