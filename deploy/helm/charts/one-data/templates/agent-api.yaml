{{- if .Values.agentApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "one-data-studio.fullname" . }}-agent-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent-api
spec:
  {{- if not .Values.agentApi.autoscaling.enabled }}
  replicas: {{ .Values.agentApi.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "one-data-studio.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: agent-api
  template:
    metadata:
      labels:
        {{- include "one-data-studio.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: agent-api
    spec:
      {{- include "one-data-studio.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "one-data-studio.serviceAccountName" . }}
      containers:
        - name: agent-api
          image: "{{ .Values.global.imageRegistry }}{{ .Values.agentApi.image.repository }}:{{ .Values.agentApi.image.tag }}"
          imagePullPolicy: {{ .Values.agentApi.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.agentApi.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.agentApi.resources | nindent 12 }}
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MYSQL_USER
                  optional: true
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MYSQL_PASSWORD
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: REDIS_PASSWORD
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: JWT_SECRET_KEY
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MINIO_SECRET_KEY
            {{- range $key, $value := .Values.agentApi.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-data-studio.fullname" . }}-agent-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent-api
spec:
  type: {{ .Values.agentApi.service.type }}
  ports:
    - port: {{ .Values.agentApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "one-data-studio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: agent-api
{{- if .Values.agentApi.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "one-data-studio.fullname" . }}-agent-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "one-data-studio.fullname" . }}-agent-api
  minReplicas: {{ .Values.agentApi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.agentApi.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.agentApi.autoscaling.targetCPUUtilization }}
{{- end }}
{{- end }}
