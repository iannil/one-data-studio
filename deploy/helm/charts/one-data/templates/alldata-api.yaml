{{- if .Values.alldataApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "one-data-studio.fullname" . }}-alldata-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: alldata-api
spec:
  {{- if not .Values.alldataApi.autoscaling.enabled }}
  replicas: {{ .Values.alldataApi.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "one-data-studio.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: alldata-api
  template:
    metadata:
      labels:
        {{- include "one-data-studio.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: alldata-api
    spec:
      {{- include "one-data-studio.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "one-data-studio.serviceAccountName" . }}
      containers:
        - name: alldata-api
          image: "{{ .Values.global.imageRegistry }}{{ .Values.alldataApi.image.repository }}:{{ .Values.alldataApi.image.tag }}"
          imagePullPolicy: {{ .Values.alldataApi.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.alldataApi.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.alldataApi.resources | nindent 12 }}
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MYSQL_USER
                  optional: true
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MYSQL_PASSWORD
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: REDIS_PASSWORD
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: JWT_SECRET_KEY
            {{- range $key, $value := .Values.alldataApi.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-data-studio.fullname" . }}-alldata-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: alldata-api
spec:
  type: {{ .Values.alldataApi.service.type }}
  ports:
    - port: {{ .Values.alldataApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "one-data-studio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: alldata-api
{{- if .Values.alldataApi.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "one-data-studio.fullname" . }}-alldata-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: alldata-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "one-data-studio.fullname" . }}-alldata-api
  minReplicas: {{ .Values.alldataApi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.alldataApi.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.alldataApi.autoscaling.targetCPUUtilization }}
{{- end }}
{{- end }}
