{{- if .Values.bishengApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "one-data-studio.fullname" . }}-bisheng-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: bisheng-api
spec:
  {{- if not .Values.bishengApi.autoscaling.enabled }}
  replicas: {{ .Values.bishengApi.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "one-data-studio.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: bisheng-api
  template:
    metadata:
      labels:
        {{- include "one-data-studio.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: bisheng-api
    spec:
      {{- include "one-data-studio.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "one-data-studio.serviceAccountName" . }}
      containers:
        - name: bisheng-api
          image: "{{ .Values.global.imageRegistry }}{{ .Values.bishengApi.image.repository }}:{{ .Values.bishengApi.image.tag }}"
          imagePullPolicy: {{ .Values.bishengApi.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.bishengApi.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.bishengApi.resources | nindent 12 }}
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MYSQL_USER
                  optional: true
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MYSQL_PASSWORD
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: REDIS_PASSWORD
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: JWT_SECRET_KEY
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: one-data-secrets
                  key: MINIO_SECRET_KEY
            {{- range $key, $value := .Values.bishengApi.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-data-studio.fullname" . }}-bisheng-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: bisheng-api
spec:
  type: {{ .Values.bishengApi.service.type }}
  ports:
    - port: {{ .Values.bishengApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "one-data-studio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: bisheng-api
{{- if .Values.bishengApi.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "one-data-studio.fullname" . }}-bisheng-api
  labels:
    {{- include "one-data-studio.labels" . | nindent 4 }}
    app.kubernetes.io/component: bisheng-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "one-data-studio.fullname" . }}-bisheng-api
  minReplicas: {{ .Values.bishengApi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.bishengApi.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.bishengApi.autoscaling.targetCPUUtilization }}
{{- end }}
{{- end }}
