# ONE-DATA-STUDIO Secrets Template
# IMPORTANT: This is a TEMPLATE file. Do NOT commit actual secrets!
# Use Kubernetes Secrets management, HashiCorp Vault, or cloud-native secrets managers.
#
# To create secrets in production:
#   kubectl create secret generic one-data-secrets \
#     --from-literal=MYSQL_PASSWORD='<secure-password>' \
#     --from-literal=MYSQL_ROOT_PASSWORD='<secure-root-password>' \
#     --from-literal=REDIS_PASSWORD='<secure-redis-password>' \
#     --from-literal=MINIO_ROOT_USER='<minio-user>' \
#     --from-literal=MINIO_ROOT_PASSWORD='<secure-minio-password>' \
#     --from-literal=JWT_SECRET_KEY='<secure-jwt-secret>' \
#     --from-literal=KEYCLOAK_CLIENT_SECRET='<keycloak-secret>' \
#     -n one-data-studio

apiVersion: v1
kind: Secret
metadata:
  name: one-data-secrets
  namespace: one-data-studio
  labels:
    app.kubernetes.io/name: one-data-studio
type: Opaque
stringData:
  # SECURITY: Replace these placeholder values with actual secrets
  # DO NOT use these default values in production!

  # Database credentials
  MYSQL_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
  MYSQL_ROOT_PASSWORD: "REPLACE_WITH_SECURE_ROOT_PASSWORD"

  # Redis password
  REDIS_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"

  # MinIO credentials
  MINIO_ROOT_USER: "REPLACE_WITH_SECURE_USER"
  MINIO_ROOT_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
  MINIO_ACCESS_KEY: "REPLACE_WITH_SECURE_KEY"
  MINIO_SECRET_KEY: "REPLACE_WITH_SECURE_SECRET"

  # JWT secret (use: openssl rand -base64 32)
  JWT_SECRET_KEY: "REPLACE_WITH_SECURE_JWT_SECRET"

  # Keycloak client secret
  KEYCLOAK_CLIENT_SECRET: "REPLACE_WITH_KEYCLOAK_SECRET"

  # OpenAI API key (optional)
  OPENAI_API_KEY: ""
