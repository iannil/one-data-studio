# 工作流使用指南
# ONE-DATA-STUDIO

## 概述

工作流是 ONE-DATA-STUDIO 的核心功能，允许用户通过可视化方式编排 AI 应用。

## 基本概念

### 节点类型

| 类型 | 说明 | 图标 |
|------|------|------|
| 输入节点 | 接收用户输入 | 📥 |
| 输出节点 | 返回处理结果 | 📤 |
| LLM 节点 | 调用大语言模型 | 🤖 |
| RAG 节点 | 检索增强生成 | 🔍 |
| 条件节点 | 条件分支判断 | ⚡ |
| 代码节点 | 自定义代码执行 | 💻 |
| 并行节点 | 并行执行多个分支 | ⏸️ |
| 子流程节点 | 嵌套工作流 | 📦 |
| 缓存节点 | 缓存中间结果 | 💾 |
| 重试节点 | 失败重试机制 | 🔄 |
| Webhook 节点 | 外部系统集成 | 🔗 |

### 连接线

- **数据流**：节点之间的数据传递
- **控制流**：执行顺序控制

## 创建工作流

### 步骤 1：新建工作流

1. 导航到 **工作流** 页面
2. 点击 **创建工作流**
3. 输入工作流名称和描述

### 步骤 2：添加节点

1. 从左侧面板拖拽节点到画布
2. 点击节点进行配置
3. 连接节点之间的数据流

### 步骤 3：配置节点

**LLM 节点配置示例**：
```json
{
  "model": "gpt-4o-mini",
  "temperature": 0.7,
  "max_tokens": 2000,
  "system_prompt": "你是一个专业的数据分析助手",
  "input_variable": "{{input.query}}"
}
```

### 步骤 4：测试运行

1. 点击 **测试** 按钮
2. 输入测试数据
3. 查看执行结果和日志

### 步骤 5：发布工作流

1. 点击 **保存**
2. 点击 **发布**
3. 选择发布版本号

## 高级功能

### 并行执行

使用并行节点同时执行多个任务：

```yaml
parallel_node:
  branches:
    - name: "分支A"
      nodes: [node1, node2]
    - name: "分支B"
      nodes: [node3, node4]
  wait_all: true  # 等待所有分支完成
```

### 条件分支

根据条件选择执行路径：

```yaml
condition_node:
  conditions:
    - expression: "{{input.score}} >= 90"
      next: "high_score_handler"
    - expression: "{{input.score}} >= 60"
      next: "medium_score_handler"
  default: "low_score_handler"
```

### 子流程

复用已有工作流：

```yaml
subflow_node:
  workflow_id: "wf-123456"
  input_mapping:
    query: "{{input.user_query}}"
  output_mapping:
    result: "{{subflow.output}}"
```

### 缓存策略

缓存耗时操作的结果：

```yaml
cache_node:
  cache_key: "embedding:{{input.text_hash}}"
  ttl: 3600  # 1小时
  strategy: "redis"  # redis 或 memory
```

### 重试机制

处理临时性失败：

```yaml
retry_node:
  max_retries: 3
  retry_delay: 1000  # 毫秒
  exponential_backoff: true
  retry_on:
    - "timeout"
    - "rate_limit"
```

## 最佳实践

### 1. 模块化设计

- 将复杂逻辑拆分为子流程
- 每个节点只做一件事
- 使用有意义的节点名称

### 2. 错误处理

- 为关键节点添加重试机制
- 使用条件节点处理异常情况
- 记录详细的执行日志

### 3. 性能优化

- 使用缓存节点减少重复计算
- 使用并行节点提高吞吐量
- 避免不必要的 LLM 调用

### 4. 版本管理

- 发布前充分测试
- 使用语义化版本号
- 保留历史版本以便回滚

## 调试技巧

### 查看执行日志

```bash
# API 方式
curl https://api.example.com/api/v1/workflows/{id}/executions/{exec_id}/logs \
  -H "Authorization: Bearer $TOKEN"
```

### 单步执行

1. 在节点上右键
2. 选择 **执行到此节点**
3. 查看中间结果

### 变量检查

使用调试节点输出变量值：
```yaml
debug_node:
  output:
    - "{{input}}"
    - "{{previous_node.output}}"
```

## 常见问题

### Q: 如何传递变量？

使用 `{{变量名}}` 语法引用变量：
- `{{input.query}}` - 输入参数
- `{{node_id.output}}` - 上游节点输出
- `{{env.API_KEY}}` - 环境变量

### Q: 如何处理大量数据？

使用批处理模式：
```yaml
batch_node:
  input: "{{input.items}}"
  batch_size: 10
  parallel: true
```

### Q: 如何集成外部 API？

使用 Webhook 节点：
```yaml
webhook_node:
  url: "https://api.external.com/endpoint"
  method: "POST"
  headers:
    Authorization: "Bearer {{env.EXTERNAL_API_KEY}}"
  body: "{{input.data}}"
```

## 下一步

- [Agent 使用指南](./agent-guide.md)
- [API 参考](../02-integration/api-reference.md)
