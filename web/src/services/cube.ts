/**
 * Cube 服务 (已废弃)
 *
 * 此模块为向后兼容保留，实际功能已迁移到 model.ts (model_api)
 * 请在新代码中使用 import from '@/services/model' 替代
 *
 * @deprecated Use '@/services/model' instead
 */

// Re-export everything from model.ts
export * from './model';
export { default } from './model';
