/**
 * AllData 服务 (已废弃)
 *
 * 此模块为向后兼容保留，实际功能已迁移到 data.ts (data_api)
 * 请在新代码中使用 import from '@/services/data' 替代
 *
 * @deprecated Use '@/services/data' instead
 */

// Re-export everything from data.ts
export * from './data';
export { default } from './data';
